# NCM TUI TODO

基于当前项目状态与本地 Pigma 源码对比整理。优先保证浏览、歌词和播放互不阻塞。列表与导航按事件刷新，转圈约 10 Hz；进度条单独约 60 Hz 绘制，进度函数保持至少 180 Hz 采样精度。

## 已完成

- [x] 使用 Miller Columns 横向级联浏览歌单、专辑、歌手和歌曲。
- [x] 打开下一级内容时保留左侧栏及其选择位置。
- [x] `Esc` 仅收起当前级联栏。
- [x] 为每个栏增加 `Loading / Ready / Error` 独立生命周期。
- [x] 使用栏稳定 ID 丢弃迟到的异步响应，并在收栏时取消对应任务。
- [x] 将加载错误限制在所属栏内，不污染根内容和全局状态。
- [x] 增加级联 breadcrumb，并在宽度不足时优先保留当前路径尾部。
- [x] 列标题显示当前位置和总数。
- [x] 长列表显示垂直 scrollbar。
- [x] 歌词默认作为独立右栏，不阻塞歌单和单曲浏览。
- [x] `l` 聚焦并放大歌词，再次按下恢复此前浏览焦点。
- [x] `h` 可彻底关闭歌词栏，状态写入 `.ncm-tui/ui.toml`。
- [x] 收藏页（创建/订阅歌单、专辑、歌手）按页加载并预取，不再一次拉完全库。
- [x] 「我喜欢的音乐」登录后打开网易云红心歌单。
- [x] 登录恢复后自动重开当前收藏入口，避免空白页。
- [x] 进度条与列表分频：播放中只重绘播放器，约 60 Hz。
- [x] 左右键只负责切换相邻栏。
- [x] 将快退和快进改为 `[` / `]`，避免左右键误触。
- [x] 未播放音乐时进度保持为零，不在启动后无意义计时。
- [x] 空闲时不空转刷新；播放时进度条约 60 Hz，其余 UI 事件驱动。
- [x] 播放进度平滑函数通过 180 Hz 采样精度测试。
- [x] 合并在线歌单详情与歌曲加载入口，减少分散请求。
- [x] 将本地音乐查询和扫描移出 UI 线程。
- [x] 增加本地 FTS 搜索、后台扫描、统计和曲目详情。

## P0：直接改善卡顿与交互

- [x] 为在线歌单、云盘和搜索结果实现真正的分页及按需加载。
  - [x] 定义 `PaginationInfo`：`offset`、`limit`、`has_more`、`total`、`loading`。
  - [x] 分页状态归属具体栏，而不是全局页面。
  - [x] 接近列表末尾时预取下一页。
  - [x] 合并分页结果时保持服务端顺序并稳定去重。
  - [x] 切换或收起栏后丢弃旧分页响应。
  - [ ] 云盘浏览（当前没有云盘入口，待 P2 多源/云盘一起做）。
- [ ] 建立分层缓存。
  - [x] 歌单 catalog 缓存：先拿 ID 和详情里已有曲目，再按页补全。
  - [x] 启动预热不再拉完整歌单曲目。
  - [ ] 内容列表和详情 TTL 缓存。
  - [ ] 音频 URL 缓存及过期刷新。
  - [ ] 歌词缓存。
  - [ ] 封面缓存。
  - [ ] 本地搜索索引缓存。
  - [ ] 提供缓存容量、过期和手动清理策略。
- [x] 增加 Command Palette。
  - [x] 支持路由跳转。
  - [x] 支持扫描、整理、队列和播放命令。
  - [x] 支持模糊搜索和键盘选择。
  - [ ] 主题切换命令（等运行时主题）。
- [x] 将局部反馈组件化。
  - [x] Toast：成功、警告和非阻塞错误。
  - [ ] Skeleton：列表首次加载和分页加载。
  - [x] 保留栏内 Error 状态和重试入口。
- [x] 增加离线模式，允许浏览并播放已有缓存和本地音乐。

## P1：本地音乐管理，无需数据库迁移

- [ ] 暴露 `collections / collection_tracks` 查询 API。
- [ ] 在 Miller Columns 中按本地歌单、专辑、歌手逐层浏览。
- [ ] 保留 collection 中的原始歌曲顺序。
- [ ] 增加分组入口：年份、格式、来源、目录。
- [ ] 增加排序：最近添加、最近播放、最多播放、标题、大小、时长、碟号、轨号。
- [ ] 增加智能视图。
  - [ ] 未播放。
  - [ ] 常听。
  - [ ] 最近添加。
  - [ ] 无专辑或元数据不完整。
  - [ ] 大文件。
  - [ ] 缺失文件。
- [ ] 增加缺失文件管理。
  - [ ] 单独显示 `missing` 曲目。
  - [ ] 重新扫描。
  - [ ] 手动重新关联路径。
- [ ] 暴露 `media_locations` 多位置管理。
  - [ ] 显示同一曲目的所有文件位置。
  - [ ] 标记不存在的位置。
  - [ ] 选择优先播放版本。
- [ ] 使用 `audio_md5` 提供重复文件候选检测。
- [ ] 使用标题、歌手和时长提供弱匹配重复候选，并明确标注其不确定性。
- [ ] 增加完整播放历史时间线，显示每次播放时间。
- [ ] 增加队列管理：重排、批量删除、清空、保存为本地集合。
- [ ] 在 UI 中暴露专辑整理和全库整理。
- [ ] 增加格式、码率、容量、时长和播放次数统计。
- [ ] 增加显式数据库维护入口，执行文件一致性检查。
- [ ] 在详情和筛选中暴露已有元数据。
  - [ ] 年份。
  - [ ] 碟号和轨号。
  - [ ] 专辑艺术家。
  - [ ] 码率。
  - [ ] Audio MD5。
  - [ ] 封面 URL。

## P2：Pigma 已有、当前项目未实现

- [ ] 使用 queue version 避免队列快照无变化时反复重建。
- [ ] 增加 IPC 和 headless daemon。
- [ ] 增加 CLI 远程播放控制。
- [ ] 增加 Waybar 等状态栏集成。
- [ ] 增加 heartbeat/scrobble 和动态推荐队列。
- [ ] 增加多源搜索及音源回退。
- [ ] 增加云盘浏览和上传缓存歌曲。
- [ ] 支持终端图片协议封面并复用封面缓存。
- [ ] 增加运行时主题切换和配置。
- [ ] 增加多套 playerbar 布局。
- [ ] 增加启动阶段进度和诊断日志。
- [ ] 改善表格单元格动作和 breadcrumb 状态恢复。

## P3：需要数据库迁移

- [ ] 增加用户自定义标签。
- [ ] 增加星级评分。
- [ ] 持久化智能歌单规则。
- [ ] 增加单曲播放进度、书签和断点续播。
- [ ] 保存 ReplayGain、BPM、调性、响度和波形分析结果。
- [ ] 在数据库中缓存歌词正文和翻译。
- [ ] 记录跳过次数、完成率和 seek 等精细收听事件。
- [ ] 建立可撤销的文件操作事务日志。
- [ ] 增加多账号本地资料隔离。
- [ ] 明确区分本地自建歌单和云端 collection，并记录同步状态。

## 不采用的 Pigma 设计

- [x] 不采用独占歌词页面；歌词与浏览栏并存，但允许彻底隐藏歌词栏。
- [x] 不采用会破坏 Miller Columns 空间关系的动态导航栏移动。
- [x] 不以降低进度条帧率换性能；列表等区域可以事件驱动。

## 每项变更的完成标准

- [ ] 不阻塞 UI 事件循环。
- [ ] 异步结果有明确归属，并能丢弃迟到响应。
- [ ] 错误只影响所属组件或栏。
- [ ] 键盘、鼠标和帮助文本行为一致。
- [ ] 不破坏歌词栏可选隐藏、Miller Columns，以及进度 60 Hz 绘制 / 180 Hz 采样。
- [ ] 增加对应单元测试或最小集成测试。
- [ ] 通过 `cargo fmt`。
- [ ] 通过 `cargo test`。
- [ ] 通过 `cargo clippy --all-targets --all-features -- -D warnings`。
- [ ] 通过 `git diff --check`。
